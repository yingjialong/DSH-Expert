---
title: 版本变更与咨询路由：0.1.2-alpha.5 → 0.1.2-rc.1 → 0.1.3-alpha.1
status: verified_inference
mastery: L2
freshness: fresh
anchors:
  - package.json
  - packages/core/agent-loop/src/index.ts#AgentLoop.create
  - packages/session/session-persistence/src/index.ts#SessionPersistence
  - packages/session/session-persistence/src/handle.ts#SessionHandle
  - packages/session/session-persistence-jsonl/src/index.ts#acquireWriteLease
  - packages/session/session-persistence-jsonl/src/lease.ts#SessionWriteLease
  - packages/session/session-persistence-jsonl/src/generation.ts
  - packages/session/session-persistence-jsonl/tests/lease.two-process.e2e.ts
  - packages/session/session-format-v1-to-v2/src/migration.ts#sessionFormatV1ToV2
  - packages/client/file-upload/src/index.ts
  - packages/util/http-proxy/src/install.ts
  - packages/session/session-telemetry-otel/tests/egress.spec.ts
  - vendor/cordis/package.json
commit: d347e703908d0406b7a7ef80e3a0e594d86b2215
verified_at: 2026-09-05
asked_by: human
---

## 版本入口与发布渠道

本页是固定版本对照，不能把共享工作树当前文件当成表中所有版本的源码。下列渠道状态为 2026-09-05 查询快照。

| 咨询对象 | 完整 commit SHA | 发布状态与用途 |
| --- | --- | --- |
| `0.1.1-rc.2` | `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e` | 保留的旧版本咨询入口 |
| `0.1.2-alpha.5` | `db6bdc3576c2d4e7c965e8e3ed0c2a731eed87f5` | npm `alpha`，历史发布 tag |
| `0.1.2-rc.1` | `a66e4702047846cdaa10c66c9d3df3951f5ea70d` | npm `latest/next`，项目默认回答基线 |
| `0.1.3-alpha.1` | `d347e703908d0406b7a7ef80e3a0e594d86b2215` | GitHub 最新 release，本次也等于 master HEAD；npm 根包尚无此版本 |

官方发布入口：[GitHub alpha.1 release](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.3-alpha.1)、[npm 根包元数据](https://registry.npmjs.org/@deepseek-ai%2Fdsh)、[PyPI SDK 元数据](https://pypi.org/pypi/deepseek-harness-sdk/json)。PyPI SDK 为 `0.1.2rc1`，其依赖锁定 runtime-bin `==0.1.2rc1`；不从 GitHub release 推断 npm/PyPI 已同版发布。

## 本次同步与发布 diff 分开计算

- 本地 DSH master：`49a606bc5b5934603f22a26957a07dc799ab0291` → `d347e703908d0406b7a7ef80e3a0e594d86b2215`，292 提交、1780 文件（+49143 / -14402）。
- alpha.5 tag → rc.1 tag：2 提交、252 文件（+252 / -252）；不能用同期 master 差异替代发布差异。
- rc.1 tag → 0.1.3-alpha.1 tag：328 提交、2051 文件（+60491 / -27277）。这与本地快进统计不同，因为旧 master 已含部分当时未发布的改动。
- Cordis 独立镜像：`00278924a984fedfaffb4bc3d5eb7d8e76215643` → `2ceea231802cc23892b4ad10012c55c7dd4982d4`，2 提交、7 文件（+125 / -14）。DSH 的内核行为仍回各 DSH SHA 的 `vendor/cordis/`；三个发布基线的 vendor manifest 都标 `4.0.2`，版本号相同不证明源码相同。

## 已回源核验的接口断点

条件（八维坐标）：TS/JS 宿主 · 嵌入或独立 Host · 多 Session / 多进程 owner · JSONL 本地存储或自定义 persistence provider · AgentLoop 与持久化接缝复用 · 不依赖模型凭据 · macOS/Linux/Windows 的平台实现分别核验 · 版本固定为上表 SHA。

| 接缝 | rc.2 `b150a551` | 默认 rc.1 `a66e4702` | 最新 alpha.1 `d347e703` |
| --- | --- | --- | --- |
| `AgentLoop.create` | 同步返回 `Agent` | 同步返回 `Agent` | `async`，返回 `Promise<Agent>` |
| `SessionPersistence.create` | `create(meta): Promise<void>` | 增加可选 `inheritedEventCount`，仍返回 `Promise<void>` | `create(header, options?): Promise<SessionHandle>` |
| 持久化访问方式 | service 的 `append(id, events)` / `load(id)` | 仍使用上述 service 形状 | `create/open` 返回 handle，按 handle `read/append/flush/close` |
| SQLite Session provider | 发布树中存在 | 发布树中已移除 | 仍不存在 |
| `session-format-v1-to-v2` | 不存在 | 不存在 | 存在，migration 声明 `fromVersion: 1` / `toVersion: 2` |

因此，旧版宿主或 persistence provider 不能直接照搬到最新 alpha：调用方须等待异步 create，并按所用版本履行 handle 的持有与关闭责任。rc.2 或 rc.1 的同步 create 答案仍有效，限定在各自 SHA，不应被新版覆盖。以上是源码交叉验证（`verified_inference`），未运行 DSH 测试，不提升为 L3 / `fact`。

关键源码（均位于已确认的 `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness` 镜像，读取时指定表中 SHA）：

- `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/core/agent-loop/src/index.ts`：`AgentLoop.create`；rc.2 第 589 行、rc.1 第 652 行、最新 alpha 第 689 行。
- `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-persistence/src/index.ts`：`SessionPersistence.create`；rc.2 第 133 行、rc.1 第 173 行、最新 alpha 第 146 行。
- `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-persistence/src/handle.ts`：仅用最新 alpha SHA 读取本页所述合同；`append` 成功不是 crash durability，`flush` 是耐久屏障，write handle 的 `close` 完成 pending durability 并释放 ownership。

## 最新 alpha 的 owner 与格式边界

- JSONL 的 `SessionWriteLease` 使用平台内核锁排斥竞争写 owner；`read` handle 不占写锁。源码在已存在 artifact 的 write-open 获取锁，新建 Session 则到首次 materializing write 才获取。不能把 release 的简写理解成“禁止其他进程读取 Session”或未落盘 Session 已持有跨进程锁。
- 一方 `lease.two-process.e2e.ts` 包含另一进程持锁时写入失败、读取成功、持锁进程退出后接管的测试；本次只审阅源码，未执行该运行测试。Q107 的最新版部分获得此证据，旧版实际竞争行为仍未实测。
- v1→v2 migration 将 Assistant stream 按 attempt 聚合；JSONL generation 逻辑读取稳定的旧 snapshot、生成新的相邻版本 artifact，保留源 generation。格式迁移不是所有历史文件兼容性或降级可行性的实测保证。

对应源码：`/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-persistence-jsonl/src/lease.ts`（`SessionWriteLease.acquire`）、`/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-persistence-jsonl/src/generation.ts`（generation 迁移）、`/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-format-v1-to-v2/src/migration.ts`（`sessionFormatV1ToV2`）；均固定 `d347e703`。

## 新增入口与不能扩大的结论

- 相对 alpha.5 发布树新增 6 个 package manifest：`client/file-upload`、`session/session-format`、`session/session-format-catalog`、`session/session-format-v0-to-v1`、`session/session-format-v1-to-v2`、`util/http-proxy`。这里只登记路由，不声称已掌握全部新模块。
- 通用文件上传的 Host seam 从 `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/client/file-upload/src/index.ts` 查起；代理配置从 `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/util/http-proxy/src/install.ts` 查起，均固定 `d347e703`。
- Release 的“所有出站请求遵循代理”概括过宽：同 SHA 的 http-proxy 英文 README 与 `/Users/majiajun/workspace/DSH-Expert/upstream/deepseek-harness/packages/session/session-telemetry-otel/tests/egress.spec.ts:63` 明确保留 OTLP 直连路径；记录 C082，不将全局 dispatcher 推广到所有第三方 transport。
- 官方 Release 标注已知性能回退；本次未测加载耗时，不能量化影响。

## 多版本咨询验收与知识新鲜度

- 11 个 `dsh-v*` tag 的完整 SHA 与远端一致，且各 SHA 的根 manifest 版本与 tag 一致。
- 禁用 Git 隐式补取（`GIT_NO_LAZY_FETCH=1`），三个基线并发执行 `git show <SHA>:<path>`，上述两个 create 签名分别匹配预期；没有切换共享工作目录。
- rc.2 专题 16 个固定锚点存在；旧版读取新版 `handle.ts` 返回失败且无源码输出，不回退到工作树文件。检查前后上游 HEAD 均为 `d347e703`，工作区干净。
- 本轮 master diff 命中 54 页、141 个去重锚点路径：52 页此前已 stale；rc.2 固定专题保留；alpha.5 历史摘要重新核对发布 tag 并纠错。69 篇原有 stale 页继续仅作路由，不能直接给新版背书。
- 版本对象与上述接口可查询不等于正式包 install/boot、native helper、全部平台测试或全部旧笔记已通过复验。具体问题继续按固定 SHA 查证。
