---
title: 版本变更：0.1.2-alpha.2 → 0.1.2-alpha.4
status: verified_inference
mastery: L1
freshness: stale
anchors:
  - package.json
  - pnpm-lock.yaml
  - packages/core/session/src/types.ts
  - packages/session/session-persistence-sqlite/package.json
  - packages/client/connection/package.json
  - packages/code-runtime/code-runtime-python/package.json
commit: 4e84901e6471b79ec0338099867ebb4606d12bb5
verified_at: 2026-09-02
asked_by: agent
---

# 版本变更：0.1.2-alpha.2 → 0.1.2-alpha.4

## 发布基线

- alpha.2：`0a53fb55bea101816fa226bb964ae2bed71c343b`。
- alpha.3：`dd6322d604e00eec1ba5e0c8541159906a21094a`，117提交、1034文件变更。
- alpha.4：`4e84901e6471b79ec0338099867ebb4606d12bb5`，alpha.3之后297提交、2371文件变更。
- 2026-09-02查询：npm根包`alpha=0.1.2-alpha.4`，`latest=next=0.1.1-rc.2`；PyPI `deepseek-harness-sdk=0.1.2a3`。
- alpha.4 tag中共242个非private package标识；本次没有穷尽每个companion tarball，也没有执行完整install/boot/native-helper闭包验收。

## 已确认的破坏性信号

- alpha.3移除SQLite Session persistence package（`refactor(session)!: remove SQLite persistence backend`）；依赖该backend的组合必须重新核验。
- alpha.4将Session event sequence与log offset区分为不同的branded identities（`refactor(session)!: distinguish event seqs from log offsets`）；事件/分页/持久化payload与下游类型必须重新生成和核验。
- code-runtime-python在alpha.4移入experimental组；不应在未核验profile与package exports时沿用旧路径或稳定性表述。

## 新增/变化信号

- alpha.3引入whole-log turn outline、deep-history load-through与projection change token等Session/Client变化。
- alpha.4对Client conversation/chat做了大量状态与性能重构，并在base/headless/sdk profile默认暴露web fetch。
- alpha.4增强LLM custom provider headers验证与discovery复用。

## 集成方必须重验

- package family与每个正式root/subpath export。
- Session event/offset brands、history分页、projection、persistence与旧artifact reopen。
- Client connection/runtime的store、conversation timeline、stream publication与终态观察。
- profile/bundle中web fetch与code-runtime-python的真实装配。
- 目标profile的完整npm install、native helper、boot/shutdown与capability-off。

## 未知

- 本页只记录发布状态与明确breaking信号；2371文件的逐subsystem contract diff未全量复验。
- 不得用alpha.4行为反推固定rc.2；本次用户问题仍必须回`b150a551`核验。
