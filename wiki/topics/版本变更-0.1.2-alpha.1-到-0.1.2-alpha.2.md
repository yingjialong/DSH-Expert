---
title: 版本变更：0.1.2-alpha.1 → 0.1.2-alpha.2
status: verified_inference
mastery: L1
freshness: fresh
anchors:
  - package.json
  - apps/cli/package.json
  - packages/core/session/src/known-event-types.ts
  - packages/api/gateway/src/index.ts
  - packages/client/connection/package.json
  - packages/client/ui-schedule/package.json
  - packages/host/plugin-inventory/src/index.ts
  - packages/preset/agent-presets/src/index.ts
  - pnpm-lock.yaml
commit: 0a53fb55bea101816fa226bb964ae2bed71c343b
verified_at: 2026-08-30
asked_by: self
---

# 0.1.2-alpha.1 → 0.1.2-alpha.2 版本变更摘要

> 区间：`cd5ef814`→`0a53fb55`，234提交、1604文件、+27862/−14050。GitHub于2026-08-30T13:52Z发布alpha.2 prerelease；npm根包于14:10Z发布到`alpha`，`latest`/`next`仍为rc.2，PyPI SDK仍`0.1.1rc1`。发布状态在本轮查询中发生变化，以下使用最终快照。

## 官方release明确列出的变化

- Web显示connection failure、automatic retry与立即重连；conversation header显示active schedules。
- plugin inventory按conversation/global scope分组，可切换AgentPreset并跨preset搜索。
- 长history与密集live messages性能优化；answer tail显示token/耗时；菜单、滚动、tool file link、diff统计与权限本地化调整。
- 修复Node 24.0–24.11.1启动/HMR问题，减少macOS/Linux冷Session不必要filesystem检查。
- 恢复alpha.1移除的`SessionEvent.ignorable`；Remote gateway统一`RemoteError`。
- 依赖owner重组以缩减不必要peer dependencies；vendor发布到Cordis 4.0.2、Loader 1.0.3、Include 1.0.7等。

## 源码/闭包形状

- package groups仍51组；排除7个Typert测试fixture后，package manifests从247增到251。新增`@deepseek-ai/dsh-client-ui-schedule`、`@deepseek-ai/dsh-deque`、`@deepseek-ai/dsh-util-time`、`@deepseek-ai/dsh-util-values`四个tag内形状；后三者已有alpha.2 tarball，schedule在最终快照中registry仍不存在。
- 对244个非private tag标识（243个packages加根CLI）穷举registry：208个已有alpha.2，36个没有（9个registry不存在、27个已有package但无alpha.2）。因此alpha.2是正式npm预发布，但tag package family仍不能无差别视为已发布闭包。
- 大量package manifests因runtime dependency owner重构而变；tag内`exports/files`必须等正式发布后重新核tarball交集。
- `KNOWN_SESSION_EVENT_TYPES`继续是build-time仓内词汇表；alpha.2恢复unknown `ignorable:true`兼容路径，不等于新增out-of-tree required vocabulary runtime registration。
- alpha.1的controller/client-store大拓扑仍在；本页未发现ApiProxy/client-runtime回归。

## 对固定版本回答的影响

- rc.2问题继续回`b150a551`，不能用alpha.2行为反推。
- alpha.1专页仍可作为固定tag历史，但其中“HEAD=alpha.1”等动态发布断言已stale。
- 目标为alpha.2的集成需重核package exports、Remote error、connection generation、preset inventory、SessionEvent compatibility及vendor closure。

## 未知

- 36个未发布tag标识是否为有意排除、后续补发或发布失败：未知。
- 根CLI的递归安装闭包是否覆盖所有可选profile组合、各已发布tarball是否与tag manifests完全一致：本页未做install/pack闭环，未知。
- 本页L1只覆盖release与拓扑信号，不声称1604文件逐项复验。
